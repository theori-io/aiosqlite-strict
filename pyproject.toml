[project]
name = "aiosqlite-strict"
version = "1.0.0"
description = "aiosqlite + strict pydantic models"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.0",
    "pydantic>=2.12.5",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.26.0",
    "pytest-cov>=6.3.0",
]
